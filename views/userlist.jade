extends layout

block content
    h1.
        User List
    ul

        each user, i in userlist
             li

                a(href="mailto:#{user.email}")="User name: "+ user.username
                br
                p Message:  #{user.message}  Message Id:  #{ user._id}
